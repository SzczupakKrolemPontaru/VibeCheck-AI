import styled from 'styled-components';
import {FontAwesomeIcon} from '@fortawesome/react-fontawesome';

export const StyledIconForButton = styled(FontAwesomeIcon)`
    margin-right: 0.3rem;
    font-size: 1rem;
`